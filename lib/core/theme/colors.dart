import 'package:flutter/material.dart';

class AppColors {
  // Cores principais baseadas nas imagens
  static const Color verdeEscuro = Color.fromARGB(255, 0, 0, 0); // Verde mais sóbrio da imagem 1
  static const Color verdeMedia = Color.fromARGB(255, 131, 3, 3); // Verde intermediário
  static const Color amareloDourado = Color(0xFFFFC107); // Amarelo dos textos de destaque
  static const Color amareloMel = Color(0xFFFFD600); // Amarelo mais vibrante
  static const Color laranjaMel = Color(0xFFFFB74D);
  static const Color verdeFolha = Color(0xFF4CAF50);
  
  // Cores de fundo
  static const Color fundoBege = Color(0xFFFEFAE0); // Fundo bege claro da imagem 4
  static const Color begeFundo = Color(0xFFFAF7F0); // Fundo principal
  static const Color begeClaro = Color(0xFFFFF9E6); // Fundo de cards
  static const Color begeZebrado = Color(0xFFFFF5E0); // Efeito zebrado
  
  // Cores de texto e elementos
  static const Color marromFonte = Color(0xFF3E2E23); // Marrom para textos principais
  static const Color marromTexto = Color(0xFF5D4037); // Marrom para textos secundários
  static const Color marromSuave = Color(0xFF8D6E63);
  static const Color marromEscuro = Color(0xFF5D4037);
  
  // Cores neutras
  static const Color branco = Color(0xFFFFFFFF);
  static const Color cinzaClaro = Color(0xFFF5F5F5);
  static const Color cinzaMedio = Color(0xFFE0E0E0);
  static const Color cinzaEscuro = Color(0xFF757575);
  static const Color cinzaTexto = Color(0xFF424242);
  static const Color preto = Color(0xFF000000);
  
  // Cores de destaque
  static const Color verdeNevoa = Color(0xFFE8F5E9); // Verde claro para destaques
  static const Color verdeModerno = Color(0xFF2E7D32);
  static const Color verdeDestaque = Color(0xFF1B5E20);
  
  // Cores para gradientes
  static const Color gradienteAmareloMel = Color(0xFFFFD600);
static const Color gradianteLaranjaMel = Color.fromARGB(255, 224, 216, 177);
  
  // Cores de estado
  static const Color sucesso = Color(0xFF4CAF50);
  static const Color alerta = Color(0xFFFF9800);
  static const Color erro = Color(0xFFD32F2F);
  static const Color info = Color(0xFF2196F3);
  
  // Cores específicas das imagens
  static const Color vermelhoMelClaro = Color(0xFFFFB4A2); // Vermelho suave da imagem 4
  static const Color roxoModerno = Color(0xFF9C27B0);
  static const Color roxoModern = Color(0xFF8B5CF6);
  
  // Cores auxiliares
  static const Color transparente = Colors.transparent;
  static const Color textoEscuro = Color(0xFF1E1E1E);
  static const Color vermelhoLimpar = Color(0xFFDC2626);
  
  // Cores para ícones e elementos decorativos
  static const Color rosaFlor = Color(0xFFF8B4B4);
  static const Color rosaEscuro = Color(0xFFE91E63);
  static const Color cinzaRoxo = Color(0xFF6B7280);
  
  // CORES PARA O GRADIENTE
  static const Color gradienteEscuro = Color(0xFF1C1C1C);
  static const Color gradienteMedio = Color(0xFF5A5A5A);
  static const Color gradienteClaro = Color(0xFF2C2C2C);
}